/*const revString = (str) => {

    //Condition, when we pass Null it says please enter a valid string.
    if (str == null) {
        console.log("Please enter a valid string");
        return undefined;
    }
    /*The empty string("") is used as a separator, so that
    the string is split between each character.
    const arr = str.split("");
    //Reverse the order of the element in an array.
    arr.reverse();
    //Returns the array as a string.
    str = arr.join("");
    console.log(str);
}

revString(null); //Please enter a valid string
revString("The quick brown fox jumped over the lazy dog"); //god yzal eht revo depmuj xof nworb kciuq ehT*/
